package uptimerobot

import (
    "context"
    "github.com/turbot/steampipe-plugin-sdk/plugin"
)

func Plugin(ctx context.Context) *plugin.Plugin {
    p := &plugin.Plugin{}
    // TODO: Configure Plugin (Name, ConnectionConfig, TableMap, etc)
    return p
}